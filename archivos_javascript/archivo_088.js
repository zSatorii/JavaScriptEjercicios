// Notación literal
const patron1 = /abc/;

// Constructor RegExp
const patron2 = new RegExp('abc');

// Con banderas (flags)
const patronInsensible = /abc/i; // i = insensible a mayúsculas/minúsculas
const patronGlobal = /abc/g; // g = encuentra todas las coincidencias
